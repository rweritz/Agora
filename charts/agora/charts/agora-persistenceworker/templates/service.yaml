apiVersion: v1
kind: Service
metadata:
  name: {{ include "agora-persistenceworker.fullname" . }}
  labels:
    {{- include "agora-persistenceworker.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "agora-persistenceworker.selectorLabels" . | nindent 4 }}
