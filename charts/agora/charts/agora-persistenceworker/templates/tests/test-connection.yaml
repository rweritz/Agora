apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "agora-persistenceworker.fullname" . }}-test-connection"
  labels:
    {{- include "agora-persistenceworker.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "agora-persistenceworker.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
