apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "agora-simulator.fullname" . }}-test-connection"
  labels:
    {{- include "agora-simulator.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "agora-simulator.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
